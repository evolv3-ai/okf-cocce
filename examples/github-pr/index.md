---
type: Reference
title: github-pr
description: Example OKF-COCCE workflow.
tags: [example]
---

# github-pr

This example shows a small OKF-COCCE pattern.

## Files

- `index.md` is the example entrypoint.
- Concept documents use YAML frontmatter with a non-empty `type` field.

Before creating a pull request, identify `repo`, `repo_pull_request`, `repo_pull_request_create`, a GitHub or human approval connection, and a PR or validation event.
