---
type: Reference
title: mcp-filesystem
description: Example OKF-COCCE workflow.
tags: [example]
---

# mcp-filesystem

This example shows a small OKF-COCCE pattern.

## Files

- `index.md` is the example entrypoint.
- Concept documents use YAML frontmatter with a non-empty `type` field.

Before reading through an MCP filesystem server, identify `mcp`, `mcp_server`, `mcp_tool_invoke`, the bounded filesystem connection, and any audit event required by policy.
